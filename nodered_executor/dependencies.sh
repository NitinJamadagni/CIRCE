printf "add this or the equivalent path of your node installation on nvm to your ~/.bashrc:\n'export NODE_MODULES_ROOT=~/.nvm/versions/node/v6.11.4/lib/node_modules'\nRemember to 'source ~/.bashrc' later"
sudo npm install -g forever
sudo npm install -g randomstring
sudo npm install -g express
sudo npm install -g node-red-embedded-start
sudo npm install -g --unsafe-perm node-red
